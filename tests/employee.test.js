const index = require('../src/index');

describe('employee', () => {
    describe('name', () => {
        it('contains users name', () => {
            const name = new name('')
        });
    });
    describe('id', () => {
        it('contains users identification', () => {
            
        });
    });
    describe('email', () => {
        it('contains users email', () => {
            
        });
    });
    describe('role', () => {
        it('contains users role', () => {
            
        });
    });
})
// test('checks for employee name, id, email, and role', () => {
//     expect().toContain(str)
// })